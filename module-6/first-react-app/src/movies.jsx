// function MoviesList() {

//     const handleSortByYear = () => {
//         const handleSortByYear = ( => {
//             const sortedMovies = currentMovies.slice().sort((movieA, movieB) => {
//                 if (movieA.year < movieB.year) {
//                     return -1;
//                 } else {
//                     return 0;
//                 }
//             })
//         })

//         serCurrentMovies(sortedMovies);    
//     }

//     const handleSortByTitle = () => {
//         const handleSortByTitle = ( => {
//             const sortedTitles = currentMovies.slice().sort((movieA, movieB) => {
//                 if (movieA.title.toUpperCase() < movieB.title.toUpperCase()) {
//                     return -1;
//                 } else {
//                     return 0;
//                 }
//             })
//         })
        
//         serCurrentMovies(sortedTitles);    
//     }
// }